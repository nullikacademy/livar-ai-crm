# Local configuration — holds the Supabase service_role secret key.
# Never commit this: copy config/config.example.php to config/config.php instead.
/config/config.php

# PHP / server
error_log
*.log

# Editors & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Downloaded/staged WhatsApp media. Never committed: it is customer
# content, it grows without bound, and storage/.htaccess is the only
# part of storage/ that belongs in git.
/storage/media/
